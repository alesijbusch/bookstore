import styled from "styled-components";
import { Media } from "../../ui/media";

const HomeWrapper = styled.section`
  margin-top: 48px;
  margin-bottom: 72px;
  ${Media.SM} {
    margin-top: 36px;
    margin-bottom: 56px;
  }
`;

export { HomeWrapper };
